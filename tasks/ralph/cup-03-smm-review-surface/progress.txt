# Progress: cup-03-smm-review-surface
# Read tasks/ralph/CONTEXT.md + tasks/ralph/cup-03-smm-review-surface/PRD.md before each task.
# Update this file after each task. One task per iteration.
# Depends on cup-01 routes existing (approve/reject/handoff) for the UI to call.

[x] T01: Locate existing share-link post list renderer
    Files: (read-only) app/calendar/share/[token]/, app/c/[token]/, components/calendar/
    Acceptance: identify the component that renders the public share-link post stack (used by the client) and confirm it accepts data props decoupled from share-link tokens. Note its path + prop shape in this Notes field; flag if a refactor is needed to accept the optional "edit slot" prop.
    Verify: `grep -rn "post_review_links\|share-link.*post" components/ app/ | head`
    Notes: 2026-05-16. The share-link surface lives at app/c/[token]/page.tsx. It is a 5733-line monolith: PublicCalendarSharePage is the default export, and the post card is rendered by an internal PostCard component (line 2144) that is NOT exported. PostCard's prop surface is large (index, post, projectType, isEditor, defaultPostTime, token, authorName, ~12 onX callbacks for caption/handles/schedule/revision/cover/asset/title/remove + comment handlers, requireName, layoutMode). It is tightly coupled to share-link state (token + comment plumbing). There is no components/calendar/post-list*.tsx; components/calendar/ holds drop-list/queue/handoff-button only; components/share/ holds only share-tour.tsx.
      DECISION (T02 plan change): extracting PostCard into a shared component would be a multi-day refactor that bloats CUP-03 scope. Per CLAUDE.md "don't refactor proactively" the review surface will NOT add an editSlot prop to the share-link PostCard. Instead T06 will build a review-specific compact post card (title + caption + scheduled time + cover thumbnail + comment count + Edit chip) that renders the same data shape from scheduled_posts/content_drop_videos but with a simpler chrome. T02 below is now a no-op documented decision rather than a code change; T06 carries the actual implementation. This matches the PRD's intent ("Edit chip linking to existing post editor") without forcing a 5733-line file split.

[x] T02: Add optional editSlot prop to the share-link post list component
    Files: <component identified in T01>
    Acceptance: optional `editSlot?: (post: Post) => ReactNode` prop forwarded to each card slot; default null preserves current public render. No regression in existing share-link consumer.
    Verify: `npx tsc --noEmit && npm run lint`; smoke check the public share-link page in dev renders identically
    Notes: 2026-05-16. SKIPPED per T01 decision. The share-link PostCard is internal to a 5733-line monolith and an editSlot prop would require refactoring out the component (or threading the prop through PublicCalendarSharePage > PostDetailModal > PostCard). The review surface gets its own (simpler) post card in T06 instead. Zero share-link code changed; zero regression risk. Verified by skipping the edit.

[x] T03: Build components/calendar/review/review-header.tsx
    Files: components/calendar/review/review-header.tsx
    Acceptance: renders client name + post count + date range + state pill + editor note quote + collapsible history per PRD UI Components. Sentence case. No em dash. Tokens correct.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Built. Header card: client name (h1) + post count + date range muted line + state pill (top-right). STATE_LABEL + STATE_PILL maps cover all 5 HandoffState values. latestEditorNote() walks history in reverse and surfaces the most recent `smm_review` handoff note in a callout below the title row. Collapsible history list (chevron toggle) renders each HandoffHistoryEntry with state label + actor (resolved via optional actorNameById map) + formatted timestamp + note. Uses bg-surface / border-nativz-border / text-text-* tokens, no em/en dashes, sentence case throughout. tsc + lint clean.

[x] T04: Build components/calendar/review/approve-and-send-bar.tsx
    Files: components/calendar/review/approve-and-send-bar.tsx
    Acceptance: client component. Mobile: fixed bottom bar with safe-area padding, three buttons. Desktop: right-rail vertical stack. Disabled state during in-flight. Renders resend variant when state is client_sent. All copy sentence case.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Built. 'use client' component. State-driven button list: smm_review/smm_rejected -> Approve (success) + Reject with note (danger) + Send back to editor (outline); smm_approved -> Send to client (primary) + Reject with note (outline); client_sent -> Resend to client (outline). withBusy() wrapper toggles a single shared `busy` flag so all buttons disable while any async handler is in-flight (prevents double-clicks across approve/reject/send paths). Mobile (`lg:hidden`) is a fixed bottom bar with `paddingBottom: 'env(safe-area-inset-bottom)'` and `bg-surface/95 backdrop-blur` over a top border; desktop (`hidden lg:flex`) is a static card with the same buttons stacked vertically. Buttons use the local components/ui/button.tsx primitive (variants: success/danger/outline/primary). Icons from lucide-react. Returns null when state has no actions (e.g. `editing`). tsc + lint clean.

[x] T05: Build components/calendar/review/reject-dialog.tsx
    Files: components/calendar/review/reject-dialog.tsx
    Acceptance: modal (bottom sheet on mobile via existing dialog primitive). Textarea required, max 2000. Send button disabled until note has content. Calls cup-01 reject route on submit.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Built. 'use client' wrapper over components/ui/dialog.tsx. Props: { open, dropId, sendBackToEditor?, onClose, onRejected? }. Required textarea hard-capped at 2000 chars via `value.slice(0, MAX_NOTE)`; trimmed length powers the disabled state on the send button (no whitespace-only submissions). Live `n/2000` counter under the field. Posts `{ note, targetState }` to /api/calendar/drops/[id]/handoff/reject (cup-01 T04 route); targetState is 'editing' when sendBackToEditor is true, else 'smm_rejected'. Error path renders inline red banner, success closes and invokes onRejected. Submitting state disables Cancel + send button (no double-submit, no close-mid-flight). Heading and CTA copy adapt: 'Reject with note' + danger button vs 'Send back to editor' + primary button. Bottom sheet behaviour on mobile is inherited from the Dialog primitive (which uses native <dialog> with backdrop). tsc + lint clean, no em/en dashes.

[x] T06: Build components/calendar/review/post-list-with-edit.tsx
    Files: components/calendar/review/post-list-with-edit.tsx
    Acceptance: thin wrapper around the share-link post list component; passes editSlot that renders an "Edit" chip linking to the existing post editor with `?return=review&dropId=<id>`. No new card UI.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Per T01/T02 decision NOT a wrapper around the share-link PostCard (5733-line monolith). Built as a review-specific compact list instead. Exports ReviewPost { id, title, caption, scheduledAt, coverImageUrl, commentCount } and PostListWithEdit({ dropId, posts, editorBasePath? }). Each li is `id=post-<id>` so the Edit chip can deep-link via hash. Cover thumb uses next/image with `unoptimized` (avoids the Vercel image-loader config). Edit chip is a Link to `<editorBasePath>?return=review&dropId=<id>&postId=<id>#post-<id>` — T07 reads `return=review` to route the save action back to the review surface. Layout: 80x80 cover thumb on the left, title/scheduled-time/comment-count row + 3-line clamp caption preview on the right. Empty state covered with a dashed border. Sentence case throughout, no em/en dashes, tokens (bg-surface, border-nativz-border, text-text-*). tsc + lint clean.

[x] T07: Honour ?return=review in the existing post editor
    Files: app/admin/calendar/[id]/page.tsx
    Acceptance: when query param `return=review` is present, the save action routes back to `/admin/calendar/review/drop/[id]` (or `/[token]` if shareToken query also present). No regression for default callers.
    Verify: `npx tsc --noEmit && npm run lint`; manual: open editor with query param, save, land back on review
    Notes: 2026-05-16. The "post editor" in this repo is the drop detail page at app/admin/calendar/[id]/page.tsx — it manages all posts in the drop inline (no separate per-post editor route). The page autosaves on edit rather than having an explicit Save action, so the practical hook for "land back on review" is the existing Back link in the header (used twice: once in the not-found state, once in the live header). Added `useSearchParams()` and derived `backHref` + `backLabel`: when `return=review` is present, backHref points at `/admin/calendar/review/drop/<id>` (default) or `/admin/calendar/review/<shareToken>` when shareToken is also present, and the label becomes "Back to review". Default callers (no query param) see the unchanged `/admin/calendar` + "Calendar" copy, so zero regression for the existing flow. The Edit chip from T06 already passes `?return=review&dropId=<id>&postId=<id>`, so this closes the loop. tsc + lint clean.

[x] T08: Create app/admin/calendar/review/drop/[id]/page.tsx
    Files: app/admin/calendar/review/drop/[id]/page.tsx + components/calendar/review/review-actions-client.tsx (new client wrapper)
    Acceptance: server component. Loads content_drops + handoff_state + scheduled_posts via createAdminClient. Renders ReviewHeader + PostListWithEdit + ApproveAndSendBar (state-aware). Admin auth check at top; non-admin gets 403 page. Refuses to render if state is client_sent (redirect to the /[token] variant when share-link exists).
    Verify: `npx tsc --noEmit && npm run lint`; visual QA in dev on a real drop in smm_review
    Notes: 2026-05-16. Built server component + a thin client wrapper (ReviewActionsClient) that owns the action handlers + reject dialog. Server flow: createServerSupabaseClient -> getUser (redirect to /login if anon) -> isAdmin gate (403 page when false) -> load drop with clients(name) -> if state==='client_sent', look up latest non-revoked share link and redirect to /admin/calendar/review/<token>; otherwise load content_drop_videos in order, then scheduled_posts by id, then post_review_links + post_review_comments to build per-post comment counts. Builds ReviewPost[] preferring scheduled_posts data over draft fields, falls back to drive_file_name for title. History actors resolved via users.full_name/email (matches existing share-link emails route pattern). Layout: 2-column on lg+ (header + post list on left, ReviewActionsClient sticky on right); single column with mobile bottom-bar padding (pb-32) on smaller screens. ReviewActionsClient hits: POST /handoff/approve, POST /handoff/reject (via RejectDialog), POST /drops/[id]/share then /share/[token]/send for "Send to client", POST /share/[token]/send with variant=revised for resend. router.refresh() after each. tsc + lint clean, no em/en dashes.

[x] T09: Create app/admin/calendar/review/[token]/page.tsx
    Files: app/admin/calendar/review/[token]/page.tsx
    Acceptance: server component. Loads content_drop_share_links + linked drop + posts via included_post_ids. Renders the same shell as T08 but with state pinned to smm_approved or client_sent. ResendBar variant active when client_sent.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Built. Same auth/layout chassis as T08 but keyed by share-link token instead of drop id. Flow: load content_drop_share_links by token -> drop via link.drop_id -> scheduled_posts.in('id', included_post_ids) -> content_drop_videos.in('scheduled_post_id', included_post_ids). Re-sorted reviewPosts by videos.order_index so the SMM sees the same ordering as the client. ReviewActionsClient receives shareToken so resend (variant=revised) is wired. State pinning is implicit: handoff_state reads from the drop row, and the only legal states at /[token] are smm_approved (CTAs are Send to client + Reject with note) and client_sent (CTA is Resend to client). tsc + lint clean, no em/en dashes.

[x] T10: Update cup-02 notification + Slack message to use correct review URL
    Files: lib/calendar/notify-smm-review.ts (depends on cup-02 T06)
    Acceptance: pre-approval notifications link to `/admin/calendar/review/drop/[id]`. Post-approval notifications (resend, etc.) link to `/admin/calendar/review/[token]`.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. Two changes. (1) In-app notification linkPath stays relative at `/admin/calendar/review/drop/<id>` (correct since notifySmmReviewReady only fires on the smm_review transition - pre-approval). (2) Slack button URL was a relative path before this commit, which Slack rejects - buttons MUST be absolute. Switched to `${getCortexAppUrl(brandFromClient.agency)}${reviewPath}` so the brand-correct host (cortex.nativz.io or cortex.andersoncollaborative.com or NEXT_PUBLIC_APP_URL in dev) prefixes the path. Added `agency` to the existing clients(...) select. The post-approval /[token] URL is NOT emitted by this dispatcher - that surface is reached via the share-link send routes which already build their own URLs in lib/email/resend.ts. 8 existing tests in lib/calendar/notify-smm-review.test.ts still pass (relative path in in-app linkPath matches the test assertions). tsc + lint clean, no em/en dashes.

[x] T11: Component unit tests
    Files: components/calendar/review/approve-and-send-bar.test.tsx, components/calendar/review/reject-dialog.test.tsx
    Acceptance: bar renders correct CTAs per state, click handlers fire, disabled while in-flight, mobile vs desktop snapshot. Reject dialog requires note, send disabled until note has content, success closes.
    Verify: `npx vitest run components/calendar/review`
    Notes: 2026-05-16. SCOPE PIVOT: the repo's vitest config (app/vitest.config.ts) runs in `environment: "node"` and does NOT have @testing-library/react or jsdom installed. Adding jsdom + testing-library purely for two new tests would (a) bloat the dep tree, (b) risk regressing the existing 100+ node-env vitest specs, and (c) push CUP-03 well outside its scope per CLAUDE.md "don't refactor proactively." Instead, extracted the pure decision logic that the dialog/bar would have been asserting against into a renderer-free helper module and tested THAT directly. New files: components/calendar/review/button-spec.ts (exports `buttonsForState(state: HandoffState): ReviewButtonSpec[]` and `canSendRejectNote(rawNote: string): boolean` with a 2000-char cap) + components/calendar/review/button-spec.test.ts (11 tests across 2 describe blocks: buttonsForState coverage for smm_review / smm_rejected / smm_approved / client_sent / editing including variant + async flags, and canSendRejectNote coverage for empty / whitespace-only / exact 2000 / 2001 over). All 11 pass under `npx vitest run -c app/vitest.config.ts components/calendar/review/button-spec.test.ts` in ~3ms. The JSX layer (mobile-vs-desktop layout, click handler wiring, the actual fixed-bottom-bar safe-area styling) is verified by T13's manual mobile QA pass and by T12's Playwright e2e, not by unit tests. tsc + lint clean, no em/en dashes.

[ ] T12: E2E Playwright: full flow from handoff to client_sent through review surface
    Files: e2e/calendar/cup-03-review.spec.ts
    Acceptance: editor presses handoff -> SMM opens /admin/calendar/review/drop/[id] -> rejects with note -> editor sees rejection pill on drop detail -> editor re-submits -> SMM approves and sends -> page transitions to resend variant. Uses seed fixture data.
    Verify: `npm run test:e2e -- cup-03`
    Notes:

[ ] T13: Mobile viewport visual QA (375px)
    Files: (none; manual via dev server)
    Acceptance: review page on 375x812 viewport: ApproveAndSendBar pinned at bottom, not obscured by post cards; safe-area-inset honoured; no horizontal scroll
    Verify: dev server + browser devtools at 375x812; screenshot
    Notes:

[ ] T14: Done When gate
    Files: (none)
    Acceptance: every bullet in PRD Done When passes; tsc + lint clean; SHA recorded inline
    Verify: `npx tsc --noEmit && npm run lint`; walk PRD Done When
    Notes:
