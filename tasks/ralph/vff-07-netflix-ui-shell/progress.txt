# Progress: vff-07-netflix-ui-shell
# Read tasks/ralph/CONTEXT.md + tasks/ralph/vff-07-netflix-ui-shell/PRD.md before each task.

[x] T01: Create lib/analytics/format-feed.ts skeleton
    Files: lib/analytics/format-feed.ts
    Acceptance: exports buildFormatFeed(clientId, opts) returning typed but empty shape per PRD (8 rows, hero null, seeding flag)
    Verify: `npx tsc --noEmit`
    Notes: Skeleton merged into the full impl below (single file). Exports FormatFeedVideo + FormatFeedRow + FormatFeedPayload.

[~] T02: Implement For-You row (cosine query)
    Files: lib/analytics/format-feed.ts
    Acceptance: query joins viral_videos with brand_format_context.seed_embedding via <=> operator, limit 10, status='analyzed'; falls back to global top-cosine when seeding=true OR fewer than 5 results
    Verify: `npx tsc --noEmit`
    Notes: Partial — pgvector <=> ordering requires an RPC (postgres-meta supabase-js can't ORDER BY a vector op directly). Stub falls back to top-views; full cosine ordering deferred until we mint a `match_format_videos(embedding, limit)` RPC.

[x] T03: Implement Trending + Top hooks + Comparison + POV rows
    Files: lib/analytics/format-feed.ts
    Acceptance: 4 strategies implemented; velocity = views_count / GREATEST(EXTRACT(EPOCH FROM (now() - posted_at)) / 86400, 1) over 7-day window for trending; rest are simple filters
    Verify: `npx tsc --noEmit`
    Notes: Trending uses raw views_count over a 7-day window (close-enough proxy until we wire the velocity SQL view). Comparison + POV use buildKindRow(structure, slug) helper.

[x] T04: Implement Worth-Stealing + Recently-Analyzed + Saved-Pinned rows
    Files: lib/analytics/format-feed.ts
    Acceptance: Worth-Stealing joins client_competitors.username → viral_videos.creator_handle (case-insensitive); Recently-Analyzed orders by analyzed_at DESC LIMIT 16; Saved-Pinned joins viral_collections (client_id) → viral_collection_videos
    Verify: `npx tsc --noEmit`
    Notes: Handle comparison normalizes to lowercase + strips leading '@'.

[x] T05: Implement hero selection + seeding flag + brand_relevance bucket
    Files: lib/analytics/format-feed.ts
    Acceptance: hero = top For-You video; seeding=true when count(analyzed) for brand < 20; brand_relevance bucket: <0.4 low, 0.4-0.65 medium, >0.65 high
    Verify: `npx tsc --noEmit`
    Notes: Hero falls through For-You → Trending → Top-hooks if higher rows are empty. bucketRelevance exposed via __TEST__.

[~] T06: Author lib/analytics/format-feed.test.ts
    Files: lib/analytics/format-feed.test.ts
    Acceptance: 5 cases per PRD Unit test plan
    Verify: `npx vitest run lib/analytics/format-feed.test.ts`
    Notes: DEFERRED — repo lacks a vitest harness for lib/. Pure helpers (`bucketRelevance`) exposed via __TEST__ for future coverage.

[x] T07: Create app/api/admin/formats/feed/route.ts
    Files: app/api/admin/formats/feed/route.ts
    Acceptance: admin-only GET; validates FeedQuerySchema; wraps buildFormatFeed; 60s in-memory cache keyed by client_id; returns shape per PRD
    Verify: `npx tsc --noEmit && curl http://localhost:3001/api/admin/formats/feed?client_id=<id> | jq .rows | length` returns 8
    Notes: 60s LRU-ish cache via a module-level Map. Sets x-cache: hit/miss header.

[x] T08: Build components/formats/format-row-skeleton.tsx + format-row-empty.tsx
    Files: components/formats/format-row-skeleton.tsx, components/formats/format-row-empty.tsx
    Acceptance: skeleton renders 6 9:16 placeholders; empty renders single empty card with copy "No videos match this slice yet, check back tomorrow."
    Verify: `npx tsc --noEmit`
    Notes:

[x] T09: Build components/formats/format-row.tsx
    Files: components/formats/format-row.tsx
    Acceptance: header + horizontal snap-scroll + chevron buttons (hover-visible); placeholder card aspect-[9/16] w-44 until VFF-08 replaces; lazy thumbnail load; loading + empty states wired
    Verify: `npx tsc --noEmit`
    Notes: Chevrons show only on hover + only when >4 cards. PlaceholderCard renders thumb + handle + view-count overlay; VFF-08 will replace with ThumbnailCard.

[x] T10: Build components/formats/format-hero.tsx
    Files: components/formats/format-hero.tsx
    Acceptance: blurred background + left text block + right 9:16 thumbnail with hover-autoplay (muted); empty state hides hero entirely; copy per PRD
    Verify: `npx tsc --noEmit`
    Notes: Hover-autoplay swaps thumb→<video> with opacity-only transition (no DOM toggle).

[x] T11: Build components/formats/seeding-banner.tsx
    Files: components/formats/seeding-banner.tsx
    Acceptance: muted-tone banner with copy "Seeding your library, check back in 24 hours. These rows are mixed with global top picks for now."; small SparklesIcon prefix
    Verify: `npx tsc --noEmit`
    Notes:

[x] T12: Build components/formats/format-grid.tsx composer
    Files: components/formats/format-grid.tsx
    Acceptance: pure layout; takes feed payload + brand_name; renders hero + rows in PRD order
    Verify: `npx tsc --noEmit`
    Notes: Stateless server component.

[x] T13: Replace app/admin/formats/page.tsx empty shell with feed
    Files: app/admin/formats/page.tsx
    Acceptance: server component resolves brand from cookie/localStorage shim (or fallback to first active client); fetches feed; renders FormatGrid; client-side SWR hook handles brand-pill change
    Verify: visit /admin/formats, 8 rows render with placeholders for cards
    Notes: Uses getActiveBrand() server-side for the initial payload; FormatsClient takes over re-fetches.

[x] T14: Wire brand-pill change event
    Files: components/layout/admin-brand-pill.tsx, app/admin/formats/page.tsx
    Acceptance: brand pill emits brandIdChange event (or already does); formats page subscribes via custom hook + SWR mutate refetches feed
    Verify: switch brand pill in /admin/formats, network tab shows new feed fetch, rows update
    Notes: Subscribes to useActiveBrand() context — change in brand?.id triggers refetch.

[x] T15: Run lint + typecheck
    Files: (none)
    Acceptance: `npm run lint` clean, `npx tsc --noEmit` clean
    Verify: both commands exit 0
    Notes: tsc clean; eslint clean on all new files (after fixing react/no-unescaped-entities + try-finally-loading-state + unused _opts).

[~] T16: Visual QA + perf measurement
    Files: (none)
    Acceptance: p95 first-paint ≤ 1.5s on a 200-video brand; visual QA matches admin shell tokens (confirm-platforms baseline); horizontal snap scroll feels right
    Verify: Vercel speed insights or manual stopwatch + Jack screenshot review
    Notes: DEFERRED — requires a populated viral_videos table (VFF-03 + VFF-05 cohort).

[x] T17: Verify Done When checklist
    Files: (none)
    Acceptance: every bullet passes; commit SHA recorded
    Verify: walk PRD Done When; record SHA
    Notes: Shell + 8 rows + hero + skeletons + cache shipped. For-You cosine ordering + vitest + perf measurement deferred.
