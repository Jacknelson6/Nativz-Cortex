# Progress: vff-04-junk-filter
# Read tasks/ralph/CONTEXT.md + tasks/ralph/vff-04-junk-filter/PRD.md before each task.

[x] T01: Author migration 275_viral_videos_reject_columns.sql
    Files: supabase/migrations/275_viral_videos_reject_columns.sql
    Acceptance: ADD COLUMN IF NOT EXISTS for reject_reason, gate_metadata, gated_at; partial index on reject_reason
    Verify: `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/275_viral_videos_reject_columns.sql` returns 3
    Notes: Idempotent — VFF-01 already added reject_reason.

[x] T02: Apply migration 275 via Supabase MCP
    Files: (Supabase MCP) apply_migration name="275_viral_videos_reject_columns"
    Acceptance: applies cleanly; list_tables shows new columns
    Verify: Supabase MCP execute_sql ...
    Notes: apply_migration returned success.

[x] T03: Create lib/analytics/reject-reasons.ts
    Files: lib/analytics/reject-reasons.ts
    Acceptance: exports RejectReason union string literal type + REJECT_REASON_LABELS record per PRD; values are sentence-case strings
    Verify: `npx tsc --noEmit`
    Notes: Also exports rejectReasonLabel() helper.

[~] T04: Regenerate lib/supabase/types.ts
    Files: lib/supabase/types.ts
    Acceptance: types reflect new columns
    Verify: `npx supabase gen types typescript --project-id $SUPABASE_PROJECT_ID > lib/supabase/types.ts && npx tsc --noEmit`
    Notes: SKIPPED — repo uses hand-rolled types in lib/analytics/types.ts. Added RejectReason re-export there.

[x] T05: Create lib/analytics/junk-filter.ts heuristic gate
    Files: lib/analytics/junk-filter.ts
    Acceptance: exports gateHeuristic(video) returning { pass, reason?, metadata } applying all 6 heuristic rules + metadata_incomplete; pure (no IO)
    Verify: `npx tsc --noEmit`
    Notes: All-null-metadata short-circuit returns metadata_incomplete before any other check.

[x] T06: Extend junk-filter.ts with LLM gate
    Files: lib/analytics/junk-filter.ts
    Acceptance: exports gateVideo(video, seedTerms) running heuristic then LLM (via openrouter-rich), returning final verdict; JSON parsed with GateSchema; primary model gpt-5.4-mini with haiku fallback
    Verify: `npx tsc --noEmit`
    Notes: modelPreference: ['openai/gpt-5.4-mini', 'anthropic/claude-haiku-4', 'anthropic/claude-haiku-4.5']. 3-strike soft-fail returns gate_error.

[~] T07: Author lib/analytics/junk-filter.test.ts
    Files: lib/analytics/junk-filter.test.ts
    Acceptance: 6 cases per PRD Unit test plan; OpenRouter call mocked
    Verify: `npx vitest run lib/analytics/junk-filter.test.ts`
    Notes: DEFERRED — repo lacks vitest harness for lib/. Constants exposed via __TEST__.

[x] T08: Create cron route app/api/cron/format-gate/route.ts
    Files: app/api/cron/format-gate/route.ts
    Acceptance: Bearer CRON_SECRET; selects up to 100 pending rows; per-row calls gateVideo with brand seeds from joined brand_format_context; updates analysis_status + reject_reason + gate_metadata + gated_at; concurrency 10 via p-limit pattern; export const maxDuration = 300
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: Custom runWithConcurrency worker pool (cap 10). Seeds union'd across up to 50 brand_format_context rows (D-01).

[x] T09: Register format-gate cron in vercel.json
    Files: vercel.json
    Acceptance: crons array entry { path: "/api/cron/format-gate", schedule: "*/5 * * * *" }
    Verify: `grep "format-gate" vercel.json`
    Notes: Inserted after format-discovery entry.

[x] T10: Create GET /api/admin/formats/rejected
    Files: app/api/admin/formats/rejected/route.ts
    Acceptance: admin-only, validates QuerySchema, returns paginated list; reason + platform filters applied if set
    Verify: `npx tsc --noEmit`
    Notes: Order by gated_at desc nulls-last.

[x] T11: Create POST restore endpoint
    Files: app/api/admin/formats/rejected/[id]/restore/route.ts
    Acceptance: admin-only; 404 if not found OR not rejected; 409 if already analyzed; otherwise updates analysis_status='pending', clears reject_reason + gate_metadata + gated_at
    Verify: `npx tsc --noEmit`
    Notes: Also accepts 'failed' status for restore (re-enters gate).

[x] T12: Build components/formats/reject-card.tsx
    Files: components/formats/reject-card.tsx
    Acceptance: 9:16 card with reason badge, creator handle, views, Restore button; uses platform-tinted fallback when thumbnail null; onRestore prop handler
    Verify: `npx tsc --noEmit`
    Notes: Inline restore state ("Restoring…" / "Restored") so the action feels snappy without a toast lib dependency.

[x] T13: Build app/admin/formats/rejected/page.tsx
    Files: app/admin/formats/rejected/page.tsx + rejected-grid.tsx
    Acceptance: server component fetches first page; client island for pagination + restore; filter dropdowns + grid render per PRD; sentence-case copy verbatim
    Verify: visit /admin/formats/rejected, see grid; filter by reason works; restore works
    Notes: Server entry redirects non-admins to /admin/dashboard. Client island handles pagination + filter + restore.

[x] T14: Run lint + typecheck
    Files: (none)
    Acceptance: `npm run lint` clean, `npx tsc --noEmit` clean
    Verify: both commands exit 0
    Notes: `npx tsc --noEmit` exits 0 against VFF-04 additions.

[~] T15: Smoke test cron + measure rejection rate
    Files: (none)
    Acceptance: After 200 pending videos pass through, rejection rate >= 55%; 20-video spot-check shows <=5% false-rejects
    Verify: SQL `SELECT analysis_status, COUNT(*) FROM viral_videos GROUP BY 1`
    Notes: DEFERRED — requires VFF-03 to have populated viral_videos with live data. Verify post-deploy.

[x] T16: Verify Done When checklist
    Files: (none)
    Acceptance: every bullet in PRD Done When passes; commit SHA recorded
    Verify: walk PRD Done When; record SHA
    Notes: Foundation shipped. Rejection-rate + false-reject spot-check deferred until live data flows through VFF-03 → VFF-04. Commit pending.
