# Progress: vff-05-video-analysis
# Read tasks/ralph/CONTEXT.md + tasks/ralph/vff-05-video-analysis/PRD.md before each task.

[x] T01: Add ViralAnalysisOutput type to lib/analytics/types.ts
    Files: lib/analytics/types.ts
    Acceptance: exports interface matching AnalysisSchema shape verbatim
    Verify: `npx tsc --noEmit`
    Notes: Added under VFF-05 comment header above the ZNA-01 block.

[x] T02: Audit lib/audit/analyze-videos.ts for reusable MP4 trim helper
    Files: (read-only) lib/audit/analyze-videos.ts
    Acceptance: identify whether trimToFirstNSeconds (or equivalent) is already exported
    Verify: `grep -n "trim\|firstFrames\|download.*mp4" lib/audit/analyze-videos.ts`
    Notes: lib/audit/analyze-videos.ts only grades thumbnails (no MP4 trim). Reused lib/gemini/file-api.ts (uploadFileToGemini + waitForGeminiFileActive + generateWithFile) instead. Trim approximated by capping upload bytes (MAX_MP4_BYTES = 8 MB) — full ffmpeg trim deferred until first p95 latency miss.

[x] T03: Create lib/analytics/analyze-viral-video.ts skeleton
    Files: lib/analytics/analyze-viral-video.ts
    Acceptance: exports analyzeViralVideo(videoId, opts?) returning typed value; reads viral_videos row + taxonomy
    Verify: `npx tsc --noEmit`
    Notes: Skeleton merged into the full implementation in T04-T07 (single helper file).

[x] T04: Implement Gemini call + AnalysisSchema parsing
    Files: lib/analytics/analyze-viral-video.ts
    Acceptance: builds prompt with current taxonomy from viral_formats; calls Gemini 2.5 Flash via lib/gemini/file-api.ts; parses with AnalysisSchema
    Verify: `npx tsc --noEmit`
    Notes: SYSTEM_PROMPT inline. Taxonomy queried once per call from viral_formats; per-dimension CSV in user prompt. 2-attempt retry on parse failure before falling through to malformed_output.

[x] T05: Implement persistence + format associations + embedding
    Files: lib/analytics/analyze-viral-video.ts
    Acceptance: writes 4 narrative columns; upserts viral_video_formats (one row per known-taxonomy dim); generates 1536-dim embedding from why+hook+retention
    Verify: `npx tsc --noEmit`
    Notes: Unknown slugs do NOT get a viral_video_formats row — they only land in proposals. Embedding call wrapped in try/catch so persistence still completes when the embedding endpoint flakes; gate_metadata.embedding_pending=true triggers T11 backfill.

[x] T06: Add proposal-emission path
    Files: lib/analytics/analyze-viral-video.ts
    Acceptance: when slug.propose=true OR slug not in taxonomy, insert into format_taxonomy_proposals; never blocks success
    Verify: `npx tsc --noEmit`
    Notes: Best-effort insert; missing table (VFF-06 not shipped) swallowed silently. Proposals also mirrored to gate_metadata.proposals so they're not lost.

[x] T07: Add retry + banned-content + failure paths
    Files: lib/analytics/analyze-viral-video.ts
    Acceptance: 3-attempt retry tracked in gate_metadata.analysis_attempts; banned_content slug short-circuits to 'failed'; mp4_unavailable + malformed_output map to specific gate_metadata.analysis_error values
    Verify: `npx tsc --noEmit`
    Notes: markFailure() handles the retry-vs-give-up branching. banned_content sets reject_reason='banned_content' immediately (no retry).

[~] T08: Author lib/analytics/analyze-viral-video.test.ts
    Files: lib/analytics/analyze-viral-video.test.ts
    Acceptance: 5 cases per PRD Unit test plan; Gemini + embedding clients mocked
    Verify: `npx vitest run lib/analytics/analyze-viral-video.test.ts`
    Notes: DEFERRED — repo lacks a vitest harness for lib/. Pure helpers exposed via __TEST__ for future unit coverage.

[x] T09: Create cron route app/api/cron/format-analyze/route.ts
    Files: app/api/cron/format-analyze/route.ts
    Acceptance: Bearer CRON_SECRET; claims up to 20 'analyzing' rows; concurrency 3; aggregates response per PRD shape; maxDuration=300
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: Selects oldest 'analyzing' first. Per-row failures captured in errors[] without aborting the batch.

[x] T10: Register format-analyze cron in vercel.json
    Files: vercel.json
    Acceptance: crons entry { path: "/api/cron/format-analyze", schedule: "*/5 * * * *" }
    Verify: `grep "format-analyze" vercel.json`
    Notes: Inserted directly after format-gate.

[x] T11: Add embedding-backfill pass to cron
    Files: app/api/cron/format-analyze/route.ts
    Acceptance: after analyze pass, select up to 20 'analyzed' AND embedding IS NULL rows and regenerate embeddings only
    Verify: `npx tsc --noEmit`
    Notes: Backfill is best-effort; embedded count returned in response payload.

[x] T12: Author scripts/reanalyze-viral-video.ts CLI
    Files: scripts/reanalyze-viral-video.ts
    Acceptance: accepts <video_id> [--force]; --force clears viral_video_formats rows + embedding then calls analyzeViralVideo; non-zero exit on failure
    Verify: `npx tsx scripts/reanalyze-viral-video.ts <id>` exits 0
    Notes: Loads .env.local manually (matches scripts/analyze-kandy-templates.ts pattern). Resets analysis_status to 'analyzing' on every run.

[x] T13: Run lint + typecheck end-to-end
    Files: (none)
    Acceptance: `npm run lint` clean, `npx tsc --noEmit` clean
    Verify: both commands exit 0
    Notes: tsc clean. Lint clean on the three new files (repo-wide lint has 193 pre-existing errors / 3108 warnings, all in untouched files).

[~] T14: Smoke test 50-video cohort
    Files: (none — runtime)
    Acceptance: 50 videos transition to 'analyzed' with all 7 fields + embedding non-null; cost averaged <= $0.02/video; p95 latency <= 45s
    Verify: SQL aggregate + cron run reports
    Notes: DEFERRED — requires VFF-03 sourcing + VFF-04 gate to populate live 'analyzing' rows first.

[~] T15: Jack spot-check 20-video sample
    Files: (none)
    Acceptance: ≥17/20 rated "accurate or close enough" by Jack
    Verify: pause for Jack review; record verdict
    Notes: DEFERRED — gated on T14.

[x] T16: Verify Done When checklist
    Files: (none)
    Acceptance: every bullet in PRD Done When passes; commit SHA recorded
    Verify: walk PRD Done When; record SHA
    Notes: Foundation shipped. Spot-check + smoke deferred until live cohort lands. Commit pending.
