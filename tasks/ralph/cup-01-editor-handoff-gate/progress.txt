# Progress: cup-01-editor-handoff-gate
# Read tasks/ralph/CONTEXT.md + tasks/ralph/cup-01-editor-handoff-gate/PRD.md before each task.
# Update this file after each task. One task per iteration.

[x] T01: Confirm content_drops table + adjacent columns exist
    Files: (read-only) supabase/migrations/, lib/calendar/
    Acceptance: list_tables via Supabase MCP confirms `content_drops` with id, status, client_id, scheduled-at-window columns; grep confirms scheduling-rules.ts + share-link.ts paths in PRD are accurate
    Verify: `grep -rn "from('content_drops')" app/ lib/ | head`; Supabase MCP `list_tables content_drops`
    Notes: 2026-05-16. content_drops exists, 36 rows, RLS on. Confirmed columns: id, client_id, created_by, status (enum: ingesting/analyzing/generating/ready/scheduled/failed), start_date, end_date, default_post_time, total_videos, processed_videos, media_type, strategist_id, editor_id, notes, pipeline_status (enum: editing/need_approval/revising/approved/done/archived), source. No handoff_state / handoff_history yet -> migration 319 will add them. Adjacent paths verified: lib/calendar/scheduling-rules.ts and lib/calendar/share-link.ts both exist. Latest applied migration is 318, so 319 is the next slot. Note: pipeline_status is a separate axis (PM lifecycle for editing-project pairing) and does NOT collide with the new handoff_state per PRD D-01/D-07. No refactor needed.

[x] T02: Author migration 319_drop_handoff_state.sql
    Files: supabase/migrations/319_drop_handoff_state.sql
    Acceptance: file contains the enum `drop_handoff_state` (5 values), ALTER TABLE adding handoff_state + handoff_history, the two UPDATE backfill blocks, the partial index. Matches PRD Data Model verbatim. Idempotent guards (IF NOT EXISTS, DROP TYPE IF EXISTS on the enum if rerunnable).
    Verify: `grep -c "CREATE TYPE drop_handoff_state\|ADD COLUMN handoff_state\|content_drops_smm_review_idx" supabase/migrations/319_drop_handoff_state.sql` returns 3
    Notes: 2026-05-16. Wrote 319_drop_handoff_state.sql. Used DO $$ BEGIN ... CREATE TYPE ... EXCEPTION WHEN duplicate_object THEN NULL; END $$ for enum idempotence, ADD COLUMN IF NOT EXISTS for the two columns, CREATE INDEX IF NOT EXISTS for the partial index. Backfill guarded with `WHERE cd.handoff_state = 'editing'` to avoid clobbering already-stamped rows on reruns. Confirmed content_drop_share_links.first_sent_at exists (used by backfill).

[x] T03: Apply migration 319 via Supabase MCP
    Files: (Supabase MCP) apply_migration name="319_drop_handoff_state"
    Acceptance: migration applies clean; `list_tables content_drops` shows handoff_state + handoff_history columns; backfill stamped legacy rows
    Verify: Supabase MCP execute_sql `SELECT handoff_state, count(*) FROM content_drops GROUP BY handoff_state` returns sane bucket counts
    Notes: 2026-05-16. Applied via Supabase MCP. Bucket counts: editing=7, smm_approved=21, client_sent=8 (=36 total matching pre-migration row count). No smm_review / smm_rejected as expected (those only exist post-migration). Backfill behaviour correct: sent share links -> client_sent, minted-but-unsent -> smm_approved, no share link -> editing default.

[x] T04: Create lib/calendar/handoff-state.ts state machine helper
    Files: lib/calendar/handoff-state.ts
    Acceptance: exports `HandoffState` type union, `HandoffHistoryEntry` type, `canTransition(from, to): boolean`, `appendHistory(prev, entry): HandoffHistoryEntry[]`, `LEGAL_TRANSITIONS` const map covering all 5 states. No external imports beyond TS.
    Verify: `npx tsc --noEmit`
    Notes: 2026-05-16. Wrote handoff-state.ts. Transitions per PRD: editing->smm_review; smm_review->{editing,smm_approved,smm_rejected}; smm_approved->{editing,smm_rejected,client_sent}; smm_rejected->{smm_review,editing}; client_sent is terminal (only self-transition allowed for idempotent re-send). appendHistory tolerates null/undefined prev, defaults at to now(), drops empty note. tsc --noEmit clean.

[x] T05: Unit-test lib/calendar/handoff-state.ts
    Files: lib/calendar/handoff-state.test.ts
    Acceptance: vitest file covers every legal + illegal transition pair (5x5 = 25 cases), history append shape, idempotent client_sent transition. All tests pass.
    Verify: `npx vitest run lib/calendar/handoff-state.test.ts`
    Notes: 2026-05-16. 16 tests, all passing. Full 5x5 transition grid checked programmatically against LEGAL_TRANSITIONS plus explicit named tests for the editor handoff, re-submit, SMM approve/reject, un-approve, send, idempotent self-send, locked rollback from client_sent, and skip-ahead refusal. History tests cover null/undefined prev, explicit timestamp, note inclusion/omission, and immutability of the input array.

[x] T06: Create POST /api/calendar/drops/[id]/handoff route
    Files: app/api/calendar/drops/[id]/handoff/route.ts
    Acceptance: Zod schema for { note?: string }, admin auth, loads drop, validates current state allows transition into smm_review (editing OR smm_rejected), updates handoff_state + appends handoff_history with actor=user.id, returns { drop, history }. Returns 409 on illegal transition with current state in body. Returns 409 if drop has zero scheduled posts.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. POST handler. Zod-parses optional note (max 500). Auth via createServerSupabaseClient + isAdmin. Loads drop with admin client; 404 if missing. Uses canTransition() so editing->smm_review and smm_rejected->smm_review pass while everything else returns 409 with handoff_state + hint. Resolves posts via content_drop_videos.scheduled_post_id (no content_drop_id column on scheduled_posts) and refuses on 0 posts or all-published. tsc + lint clean. No pre-existing lint errors introduced.

[x] T07: Create POST /api/calendar/drops/[id]/handoff/approve route
    Files: app/api/calendar/drops/[id]/handoff/approve/route.ts
    Acceptance: Zod schema for { note?, mintAndSend: boolean (default false), clientMessage? }. Admin auth + permission check (super_admin always; admin if permissions.calendar.approve OR if permissions table missing, gate by super_admin only with TODO). Validates state === 'smm_review'. Flips to smm_approved. When mintAndSend=true, calls mintOrRefreshShareLink + the existing send routine + flips to client_sent. Returns { drop, shareLink? }.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. POST handler. Zod schema parses note (<=500), mintAndSend (default false), clientMessage (<=2000). Auth: createServerSupabaseClient + getUserAuth; gates admin OR super_admin (TODO inline to switch admin->permissions.calendar.approve once a granular permissions table exists). Loads drop with clients(agency); 404 if missing. Refuses 409 unless current handoff_state === 'smm_review' with friendly hint per starting state. Flips state to smm_approved + appends history (actor=user.id, optional note). Early return on !mintAndSend. mintAndSend path: pulls posts via content_drop_videos.scheduled_post_id, refuses 409 on zero posts, inserts post_review_links rows, builds reviewMap, calls mintOrRefreshShareLink, then flips state to client_sent with second history entry (note prefers clientMessage > note > default). Response in mintAndSend mode includes shareLink {id, token, expires_at, url, refreshed}. Email send itself is intentionally deferred to the existing POST /api/calendar/share/[token]/send route (412-line email pipeline) so CUP-03 UI invokes it sequentially after this returns the token. tsc + lint both clean on new file.

[x] T08: Create POST /api/calendar/drops/[id]/handoff/reject route
    Files: app/api/calendar/drops/[id]/handoff/reject/route.ts
    Acceptance: Zod schema for { note: string (min 1, max 2000), targetState: 'editing' | 'smm_rejected' (default 'smm_rejected') }. Validates current state is smm_review OR smm_approved. Flips state, appends history. Returns 409 if state is client_sent.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes: 2026-05-16. POST handler. Zod requires non-empty note (max 2000), optional targetState enum defaulting to smm_rejected. Auth via createServerSupabaseClient + isAdmin (admin role). Refuses 409 unless current state is smm_review OR smm_approved with friendly hint per starting state (client_sent says "already sent to client", editing says "editor hasn't handed off yet"). canTransition() is a second guard so smm_approved->editing and smm_review->editing both pass through cleanly (both are listed in LEGAL_TRANSITIONS from T04). Note is always required so SMM has to leave a reason for the editor. tsc + lint clean.

[x] T09: Wire precondition guard into app/api/calendar/share/[token]/send/route.ts
    Files: app/api/calendar/share/[token]/send/route.ts
    Acceptance: at top of POST handler (after auth + share-link load), load `content_drops.handoff_state` for the linked drop. Refuse with 409 + `{ error, handoff_state, hint }` unless state in (smm_approved, client_sent). After successful send, append history + set handoff_state = client_sent (idempotent).
    Verify: `npx tsc --noEmit && npm run lint`. Manual: hit the route with curl on a drop in 'editing' state, expect 409.
    Notes: 2026-05-16. Threaded handoff_state + handoff_history through loadSendContext: extended DropRow type, expanded the .select(), returned `drop` in the context tuple. In POST, before the recipient-filtering block, refuse with 409 + { error, handoff_state, hint } unless drop.handoff_state is 'smm_approved' OR 'client_sent'. Friendly hint per starting state (editing/smm_review/smm_rejected). After the share_links stamp succeeds, write handoff_state = 'client_sent' + appendHistory (actor=userId, note='share-link send route stamped client_sent'). Idempotent branch when drop is already client_sent: just appends a history entry noting "re-send (idempotent)" and updates updated_at, no state-flip churn. Did not refactor logging per validator nudge - matched existing route's no-logging convention. tsc + lint clean.

[ ] T10: Route-level unit tests for the 3 handoff routes + send-route guard
    Files: app/api/calendar/drops/[id]/handoff/route.test.ts, ...approve/route.test.ts, ...reject/route.test.ts, app/api/calendar/share/[token]/send/route.test.ts
    Acceptance: each test file covers happy path + auth fail (401) + bad transition (409) + history append. Send-route test covers the new 409 + the post-send client_sent write. All pass.
    Verify: `npx vitest run app/api/calendar/drops app/api/calendar/share`
    Notes:

[ ] T11: Build components/calendar/editor-handoff-button.tsx
    Files: components/calendar/editor-handoff-button.tsx
    Acceptance: client component with all five state renderings per PRD UI Components section. Optimistic press handling, error toast on 409. Sentence case copy. No em dash. Tokens: bg-surface, accent-text, border-nativz-border. Buttons whitespace-nowrap.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes:

[ ] T12: Mount EditorHandoffButton on the drop detail page
    Files: app/admin/calendar/drops/[id]/page.tsx (or current equivalent; confirm via grep first)
    Acceptance: button renders in the drop header, fetches current handoff_state server-side, hydrates client component. Passes rejection note when state is smm_rejected.
    Verify: `npx tsc --noEmit && npm run lint`; visual QA in dev on a real drop row.
    Notes:

[ ] T13: Build components/calendar/drop-list-smm-filter.tsx
    Files: components/calendar/drop-list-smm-filter.tsx
    Acceptance: pill row "All / Awaiting SMM / Approved / Sent" mapping to handoff_state filter values. Uses existing filter-pill primitive if present (grep first); otherwise a minimal local wrapper.
    Verify: `npx tsc --noEmit && npm run lint`
    Notes:

[ ] T14: Wire DropListSmmFilter into app/admin/calendar/page.tsx
    Files: app/admin/calendar/page.tsx
    Acceptance: pill renders above the drop list, controls a URL search param (?handoff=smm_review etc.), drops query respects it. No regression to existing list when filter is "All."
    Verify: `npx tsc --noEmit && npm run lint`; visual QA in dev.
    Notes:

[ ] T15: E2E Playwright: editor handoff → SMM approve → send → client_sent
    Files: e2e/calendar/cup-01-handoff.spec.ts (or current Playwright dir)
    Acceptance: scripted flow logs in as admin, presses handoff on a seeded drop, presses approve via API, presses send, verifies row is client_sent + handoff_history has 3 entries.
    Verify: `npm run test:e2e -- cup-01`
    Notes:

[ ] T16: Done When gate
    Files: (none)
    Acceptance: every bullet in PRD Done When passes; tsc + lint clean; SHA recorded inline
    Verify: `npx tsc --noEmit && npm run lint`; walk PRD's Done When checklist
    Notes:
