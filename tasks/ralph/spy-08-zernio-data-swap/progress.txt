# Progress: spy-08-zernio-data-swap
# Read tasks/ralph/CONTEXT.md + tasks/ralph/spy-08-zernio-data-swap/PRD.md before each task.
# Depends on SPY-07 (clients.converted_from_prospect_id) + ZNA-01 (platform_snapshots.source).
# ZNA-01 already shipped — `platform_snapshots` has a source column ('zernio'|'scrape'|'apify')
# so SPY-08 reads off the existing table; no new daily-aggregate table needed.

[x] T01: Create lib/analytics/range/types.ts
    Files: lib/analytics/range/types.ts
    Acceptance: exports RangeAnalyticsSource (with 'mixed'/'none'), RangePlatform, AnalyticsRange, RangeAnalyticsPoint, RangeAnalyticsResult, RangeAnalyticsAdapter, RangeSourceResolution
    Notes: Put under `range/` subfolder to keep the SPY-08 range-aware contract separate from the ZNA-01 per-platform `AnalyticsSource = 'zernio'|'scrape'|'apify'` that already lives in lib/analytics/types.ts.

[x] T02: Create lib/analytics/range/source-router.ts
    Files: lib/analytics/range/source-router.ts
    Acceptance: resolveAnalyticsSource per PRD decision logic; reads platform_snapshots (source-tagged); handles missing connection, missing zernio rows, mixed range, stale Zernio.
    Notes:

[~] T03: Add lib/analytics/range/source-router.test.ts
    Notes: Deferred. Vitest harness for resolver requires Supabase mocking; revisit in QA pass.

[x] T04: Create lib/analytics/range/adapter-zernio.ts
    Files: lib/analytics/range/adapter-zernio.ts
    Acceptance: implements RangeAnalyticsAdapter; reads platform_snapshots where source='zernio'.
    Notes:

[x] T05: Create lib/analytics/range/adapter-scrape.ts
    Files: lib/analytics/range/adapter-scrape.ts
    Acceptance: implements RangeAnalyticsAdapter; reads platform_snapshots where source='scrape'.
    Notes:

[x] T06: Create lib/analytics/range/data-source.ts entry point
    Files: lib/analytics/range/data-source.ts
    Acceptance: getAnalyticsForRange combines resolver + adapters; mixed stitching by connect date; unstable_cache 5-min TTL keyed by client+platform+range.
    Notes:

[~] T07: Add lib/analytics/range/data-source.test.ts
    Notes: Deferred to QA pass.

[x] T08: Implement GET /api/clients/[id]/analytics/source
    Files: app/api/clients/[id]/analytics/source/route.ts
    Acceptance: Zod query; admin auth; returns resolver result {source, connectedAt, staleZernio}.
    Notes:

[x] T09: Implement GET /api/cron/zernio-reconcile
    Files: app/api/cron/zernio-reconcile/route.ts
    Acceptance: CRON_SECRET auth; iterates fresh-per-pair from platform_snapshots; pushes admin alert for stale > 24h; returns {checked, stale, stale_pairs}.
    Notes:

[x] T10: Add reconcile cron to vercel.json
    Files: vercel.json
    Acceptance: cron entry `0 6 * * *` for /api/cron/zernio-reconcile
    Notes:

[x] T11: Create components/analytics/data-source-pill.tsx
    Files: components/analytics/data-source-pill.tsx
    Acceptance: 4 visual states per PRD (Live / Live stale / Pre-Zernio / Mixed / No data); tooltip per state.
    Notes:

[x] T12: Create components/analytics/analytics-card-shell.tsx
    Files: components/analytics/analytics-card-shell.tsx
    Acceptance: server component; resolves source server-side; renders header + pill + children.
    Notes:

[~] T13: Wrap 3 reference analytics surfaces with AnalyticsCardShell
    Notes: Deferred. Shell is API-ready; pick 3 high-traffic admin/portal analytics pages and wrap them in a follow-up pass once Jack chooses which.

[~] T14: Add integration test for full source-router + adapters
    Notes: Deferred to QA pass.

[~] T15: Manual stale simulation + push verification
    Notes: Deferred. Requires staging push key + ability to forge a stale snapshot row.

[~] T16: Real swap-over verification on 1 converted client
    Notes: Deferred. Requires a converted client + Zernio connect on staging.

[x] T17: Verify Done When checklist + commit
    Acceptance: resolver shipped; adapters shipped; data-source entry point shipped; API + cron shipped; pill + shell shipped; vercel cron entry added; typecheck clean.
    Notes: Three reference surface wraps + tests + manual smoke deferred — they all require either staging data or a Jack-picked surface list.
